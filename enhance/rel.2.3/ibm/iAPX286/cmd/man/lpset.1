
NAME
	lpset,lpget - initialize the parallel printer driver

SYNOPSIS
	lpget device
	lpset device indentation columns lines [transparency]

DESCRIPTION
	These utilities are used to get and set teh current values
	in the LP driver that define the following:

		column indent
		columns per line
		lines per page
		transparency on or off

	Transparency mode prevents the LP driver from interpreting
	control characters and non-printing characters for graphics
	printers.

EXAMPLE
		lpset /dev/lp 0 132 66
	would set the values in the driver to their default settings.

FILES
	/dev/lp

SEE ALSO
	lp(7)

